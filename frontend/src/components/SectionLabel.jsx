export default function SectionLabel({ children }) {
  return (
    <h2 className="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-faint border-b border-line pb-2">
      {children}
    </h2>
  );
}
